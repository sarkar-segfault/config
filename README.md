# config
This is a repository containing all my config files or dotfiles.

# installation
```bash
./install.sh
```
The provided script `install.sh` will install all configuration directories to ~/.config
